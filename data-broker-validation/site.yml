---
- hosts: da_broker_val_{{ ENV }}
  user: ec2-user
  roles: 
    - yum-installs
    - pull-repo
    - pip-install
    - install-logstash
    - copy-files
    - update-schema
    - start-server
    - new-relic
  environment:
    env: "{{ ENV }}"