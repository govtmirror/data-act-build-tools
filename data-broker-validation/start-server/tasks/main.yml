---
- shell: nginx
- shell: uwsgi --ini validator_configuration.ini &
  environment:
    PYTHONPATH: "{{ ansible_env.PYTHONPATH }}:/data-act/core:/data-act/validator"
  args:
    chdir: /data-act/validator/dataactvalidator/uwsgi/
