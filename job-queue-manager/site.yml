---
- hosts: da_broker_jobmgr_{{ ENV }}
  user: ec2-user
  roles:
  - yum-installs
  - pull-repo
  - pip-install
  - install-logstash
  - copy-files
  - fsrs-cron
  - run-app
  environment:
    env: "{{ ENV }}"
